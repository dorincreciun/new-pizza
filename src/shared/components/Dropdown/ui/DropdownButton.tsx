import type {FC} from "react";
import type {IDropdownButtonProps} from "../model/types.ts";

export const DropdownButton: FC<IDropdownButtonProps> = () => {
    return(
        <button>BTN dropdown</button>
    )
}